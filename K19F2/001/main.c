#include <stdio.h>
#include <stdlib.h>
//- Viết hàm tính trung bình cộng của các phần từ trong mảng ( 6p )
/*
float geometric (int n);
int main()
{
    int n;
    printf("\ninput n: ");
    scanf("%d", &n);
    float result = geometric(n);
    printf("\n%f", result);
    return 0;
}
float geometric (int n){
    int sum = 0;
    int count = 0;
    while(n!=0){
        sum += n % 10;
        n = n / 10;
        count++;
    }
    float result = (float) sum / (float) count ;
    return result;
}
*/
int main(){

 char n;
 printf("\nnhap: ");
 scanf("%c
       ", &n);
 printf("%d", n);
}
